# 当前补证进展与未完成问题

## 已完成并局部写入论文
视觉九配置B1/2/4/8完整180单元已独立核验。MIF-Solo平均低标签收益相对Base/LAPO-Solo/LAOM-Solo为+56.17/+65.71/+53.03，各比较五seed均为正。BC/IDM仍高7.71/13.83；比B8的55.59/40.41差距小。这支持冻结latent路线的标签效率收益，没有把差距缩小归因为单一因素。

正文仅修改实验设置中的视觉预算、视觉小节和对应Fig2(b)(ii)，附录视觉表与BC解释；教授前文不变。已恢复并核查正文9页、总30页。图中BC/IDM仍显示，所有原B8配置保留；全30条件和dev MSE原始值仍在visual_summary.json。未将正在训练的新路线写成已完成优势。

BC的78978个history参数受动作监督；MIF-Solo仅18946个decoder参数受动作监督、80784个history参数冻结。MIF的history+decoder总参数反而更多。因此目前合理解释是动作梯度权限不同，而非已证明BC因更大总参数获胜，也非BC无效。

## 正在运行（已获授权）
outputs/visual_supervision_inventory_2026_09_19：120组同架构初始化×history监督控制与240组其他原家族低标签。五seed，四预算，四路；八路径训练预检、两组原冻结权重/预测复现及四新控制器闭环预检均通过。正式全量门槛含40冻结复现、80 B8decoder和80独立动作/reward重放。360新增、260复用，总620身份。

相同架构的预训练微调与随机history直接监督控制，具有完全相同的可训练参数、动作标签、decoder初始化、采样及更新数。随机控制保留公共RGB/PCA和无动作normalizer，不称完全无预训练。原冻结MIF与原BC/IDM保留。任何胜负都报告，不按回报改协议。

## 新完成的规模成本审计
results/mte_agent_resource_audit_2026_09_19/RESULTS_CN.md：4/6/8/10agent环境各步进四次通过；原Simple/Graph对8/32/128/512个子集的CPU前反向、参数和buffer实测完成。输入为合成表，不是更多agent的控制训练。Graph固定8维输入在该主机的中位前向约1.92/4.32/15.11/78.88ms，batch64、CPU单线程；其他GPU任务可能争用CPU，不做跨硬件速度排名。K指数增长且原dense变换矩阵内存为O(K²)。此项补资源证据，不宣称已解决大规模学习。

## 后续仍须独立补的科学缺口（未完成，不能写成已有证据）
1. **锁定方法的新任务确认**：先完成历史任务使用审计，选择未用于开发的场景并锁定主模型/超参/强对照，再建环境适配和全管线训练。旧OhioT1DM、同Ant换视角或旧MPE换seed都不能充当新任务。本轮视觉运行占四worker，不同时启动未经资格核验的新大型训练。
2. **更多agent控制**：现已验证4/6/8/10环境接口与编码器成本，仍需新数据、已知slot适配、h2时间窗、同标签/训练资源与闭环对照。Agent数与unlabelled N是不同轴。若联合新任务改变人数，必须区分两因素。
3. **agent-aligned视觉slot**：需要实体局部观测或新前端、并披露几何/分割先验和信息可见性。当前camera-view模型及低标签曲线都不能替代此实验。
4. **官方结构化基线**：包内只有官方LAOM相关实现，FLAM/OTF/object-centric当前是文献和接口比较，尚未完成官方同协议实跑。不能把本地raw/slot/Random-edge改名成官方基线。
5. **donor与history机制**：MPE的query-state oracle已定位输运误差；它使用模拟器，不能当部署修复。MaMuJoCo仍需实际训练donor与同动作query-state重编码的配对诊断。history动作微调本轮补的是监督适配，并不自动解决无标签interaction迁移。

以上后续按独立协议推进，不改原主线，不覆盖已完成证据，也不预设每个新增实验必为正。
