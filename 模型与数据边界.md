# 模型与数据边界

## 从输入到动作

无标签 train/dev 观测 → inverse frontend codes → 匹配的预测端点 → family encoder → history policy → 全部冻结 → 有限 target-action 标签训练 decoder → 当前/过去观测部署。

`mte/frontends.py` 定义数值 frontend；`mte/endpoints_*` 构建两端点；`closed_loop_lam_v1/unified_models.py` 定义 Simple、Graph、Tree、MIF 和 matched DeepSets；`common.py` 包含 Edge-CARA、Continuous LAM、LAPO/LAOM 适配、GRU、decoder 和 IDM。

Aux 是 `training/composition.py:FrozenPair`：拼接冻结 Base 与具名辅助历史网络输出，再训练 action decoder。Solo 不拼接 Base。Baseline、Aux 和 family 不能混成同一行汇报。

| 名称 | 输入/角色 | 接口 |
|---|---|---|
| LAPO-joint / LAOM-joint | 数值完整 joint observations 的本地适配 | `mte.frontends.train` |
| Base / entity_target | 局部实体 frontend 的目标槽历史预测 | `experiments.limited_labels.train_history` |
| Edge / Simple / Graph / Tree | 具名 matched-edge family | 冻结 common / unified_models |
| MIF | 端点、差分、时间和实体组织；不是只含差值 | 数值正式版和结构诊断版各有明确入口 |
| BC / IDM | 同预算直接监督参考及重标记参考 | `training/grounding_*.py`、视觉 ground/idm |
| Global-Joint16 | 完整系统辅助学习路线，不是两个局部 Base | `experiments.route_ablation.train_global` |

## 读取规则

数值预训练 NPZ：MPE 只接受 `positions`；Ma 只接受 `observations, valid_mask`。不得把带 action/reward/snapshot 的原始文件直接作为预训练输入。视觉预训练只打开 RGB 和允许的观察产物。`utils/access.py:guard` 在单独进程安装 numeric-read allowlist，禁止预训练导入/调用模拟器。

动作标签只在全局冻结之后单独导出；B 包含 fit 和 validation，不能只报 fit 数量。部署不读取未来帧、inverse codes 或 donor。环境 teacher 仅供数据策展或冻结后的评估，不进入预训练。

`utils/artifacts.py` 使用显式路径，不会自动退回旧端点；Ma 必须标记 `endpoint_version=constant_support_repaired`。`original_bridge` 仅用于修复前后 h1 数值回归检查，不是训练监督来源。

## 使用整理后的视觉入口

`python -m experiments.visual_control --help` 查看单阶段接口。`asset-dir` 放 `visual_config.json` 与 `labels/train`、`labels/dev`；`data-dir/rgb/{train,dev}` 放原冻结 RGB。训练以一阶段一进程运行，不能把多个读取 guard 套在同一进程。

先完成全部五种子的 frontend/features/bridge/rich/pretrain/freeze，再生成包含全部 policy/frontend 产物摘要的 global freeze record；ground/evaluate 会检查该记录。示例格式：`{"all_sources_frozen":true,"training_seeds":[908711,908712,908713,908714,908715],"artifacts":[{"path":"...","digest":"..."}]}`。这是校验记录，不是绕过全局冻结的开关。

数值入口是函数 API：显式传 suite 路径和 `configs/numeric.json`，避免旧 worker 对全局路径进行 monkey patch。批量调度服务器没有复制到新包；不要把一个接口例子误认为整套实验已经新跑完成。
