# 简易冒烟测试：通过

- MPE 和 MaMuJoCo：各 8 条真实训练轨迹、2 条开发轨迹，每阶段 2 次更新；各 11 条表示/history/decoder 路径完成。包括 Edge、Simple、Graph、Tree、MIF、Continuous LAM、LAPO/LAOM 适配及 joint/target frontend。
- 两个数值预训练进程的读取审计均为零越界；完成预训练后才打开预算标签。
- 新结构对照：两个环境各测试 Global-Joint16、Simple、Graph、MIF，共 8 条短训练路线，decoder reconstruction parity 均通过。
- 环境执行：Graph-Aux 冒烟模型在 MPE 执行 23 步，在 MaMuJoCo 执行 8 步；动作、观测和回报均有限，环境正常关闭。
- 视觉：真实 RGB frontend/augmentation 前后向通过；在线三帧历史与离线构造一致；Simple/Graph/Tree/MIF 的视觉维度前后向通过。最终 MSAA0 renderer 从 DAVIS 背景生成 3 帧，保留 4 个物理 agent，仅渲染目标视角。

测试没有发现阻断运行的错误。出现的 PyTorch 日志标量转换警告和 Gymnasium 其他环境版本提示均未导致失败。

测试使用现有最终数值端点，没有重建完整 donor/readout bank；视觉 family 张量是合成接口测试，没有重跑固定 40 轨迹的完整视觉 CLI。两次更新不检验收敛或论文性能，不能替代正式复现。

原数据、原检查点、原正式结果和冻结核心未修改。测试产物单独存放于整理工具目录的 smoke_run，ZIP 只收录测试报告，不收录这些临时模型或数据。
