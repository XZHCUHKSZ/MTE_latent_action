# 挑重点读

1. **通用模型**：`closed_loop_lam_v1/common.py` 与 `unified_models.py`；先看 EdgeCARA、MobiusSimple、MobiusGraphEdgeCARA、MIFCARAIncidenceFlow。
2. **我的 MTE 从哪里来**：`mte/frontends.py` → `mte/endpoints_mpe.py` / `endpoints_mamujoco.py`。Ma 的最终版本保护训练中恒定的输入坐标。
3. **编码以后怎样控制**：`experiments/limited_labels.py` → `training/history_*` / `representation_mamujoco.py` → `training/composition.py` → `training/control.py`。
4. **正文最新结构对照**：`experiments/route_ablation.py` 和 `training/route_representation.py`，结果看 `results/route/`。这里的训练目标和正式主矩阵分开。
5. **视觉实验**：`visual/train.py` 包含最终五种子实际使用的视觉函数；`experiments/visual_control.py` 按阶段调用，`visual/rollout.py` 使用最终渲染条件。
6. **机制证据**：动作读出看 `experiments/action_information.py`；partner interaction 看 `experiments/partner_interactions.py`；完整 coordinate pilot 入口在 `experiments/coordinate_pilot.py`。

结果查看：`python run.py --report numeric` / `visual` / `route`。原始来源在 `provenance/`，不用到历史文件夹里猜哪个版本有效。
