# 最终版本如何归并

| 原来分散的实现 | 当前目录 | 取舍 |
|---|---|---|
| `reference_lam_mte_bridge.../frontend.py` | `mte/frontends.py`、`mte/endpoints_mpe.py` | 保留最终数值 frontend；旧 Ma 未保护端点路径不开放 |
| `mamujoco_independent_branch_audit.../repair.py` | `mte/endpoints_mamujoco.py:build_full_n` | N90 使用常量输入保护后的最终端点 |
| `mamujoco_mte_recovery.../bridge.py` | 同文件 `build_small_n` | 小 N 使用相同修复思想及原 donor RNG；旧端点只读用于 h1 一致性核查 |
| `mamujoco_smalln_quality_recovery.../quality.py` | `mte/quality.py` | 用最终 train-varying 坐标质量检查，常量坐标另查 |
| `mamujoco_mte_recovery.../worker.py` 与结构训练器 | `experiments/limited_labels.py` | 明确 MIF 用修复后的结构训练器，其他 family 用完整实体接口 |
| `seed5_v1/worker.py` 调用多个名字带 pilot/family/aux 的模块 | `visual/train.py` | 提取仍在五种子正式版使用的函数，合并成一个模块；不收录这些旧脚本的 main/manager |
| `seed5_render_repair` | `visual/render.py`、`visual/rollout.py` | 最终评估 samples=0、禁用 dithering、只渲染目标视角；物理 agent 保留 |
| `mte_global_aux_scope`、`mte_family_transfer` | `experiments/route_ablation.py`、`training/route_representation.py` | 完整系统辅助对照及对应 MTE 训练目标，和正式主实验分开 |
| 动作/interaction/coordinate 诊断散落脚本 | `evaluation/probes.py`、`mte/coordinate_controls.py` | 提取固定统计器和变换；不用旧看板负责统计 |

**不能按文件名的新旧选代码。** 视觉正式版确实调用旧名函数；Ma 最终结果又确实需要后来修复。`provenance/function_sources.json` 逐符号记录来源，`result_lineage.json` 记录论文数值最终指向的结果文件。来源里保留旧名称用于追溯，不表示程序还需导入旧目录。

正式数值实验：5 个训练种子。新 X4：每环境 2 个 upstream development seeds，MPE 3 次、Ma MIF 5 次嵌套 decoder repeats。后者不能写成 5 个独立 upstream seeds。

正式 MIF 和 X4 MIF 不能共用一个隐含默认 objective。X4 使用 full-visibility value-space MSE、variance floor、0.02 covariance 的共同训练目标；正式流程保持原方法目标和各自配置。

视觉主结果对每个训练种子先平均 27 个新评估条件，再跨 5 个训练种子汇总；原有 3 个 development 条件仅进入 all30 补充统计。训练 RGB 使用 MSAA4，最终控制复评 MSAA0，这个条件差异不被代码清理抹掉。

未收录：旧 dashboards/manager、失败重试与恢复服务器、过时未修复 Ma 训练入口、早期视觉 pilot 独立任务、自动下载器、临时作图脚本、医学环境旧项目、原始数据及权重。早期双局部辅助方案不作为新结构对照入口。冻结核心文件按仓库约束不拆改。
